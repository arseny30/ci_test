dist: precise
language: cpp

before_install:
  - sudo apt-get -qq update
  #- sudo apt-get install -y libxml2-dev

addons:
  apt:
    packages:
    - cmake
    - time

compiler:
  - clang
  - gcc

script:
  - mkdir build
  - cd build
  - cmake .. && make -j3
  - ./main
